# Project Rules for Parentchild

- **Automatic GitHub Sync**: After completing any code update, feature enhancement, or bug fix, automatically run `git add`, `git commit`, and `git push origin main` to ensure GitHub is always synchronized.
